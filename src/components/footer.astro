---
export interface Props {
  // 仍然接受 linkLabels 以兼容现有页面，但当前不再渲染任何页脚内容
  linkLabels: Record<string, string>;
}
---

<!--
  全局页脚目前不需要任何可见内容，所以 Footer 组件返回一个空容器。
  这样可以在不修改各个页面的前提下，统一移除底部的图标和背景。
-->
<footer class="h-0"></footer>
