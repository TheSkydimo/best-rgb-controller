---
import "@fontsource-variable/inter";
import Footer from "~/components/footer.astro";
import GoogleAnalytics from "~/components/google-analytics.astro";
import Header from "~/components/header.astro";
import LanguagePreference from "~/components/language-preference.astro";
import { DEFAULT_LOCALE } from "~/i18n/config";
import { buildPageData } from "~/i18n/page-data";
import "~/styles/index.css";

const { generator, site } = Astro;
const locale = DEFAULT_LOCALE;
const pageData = buildPageData(locale, site);
const { copy, languageOptions } = pageData;

const PAGE_TITLE = "How to Sync RGB Lights Across Different Brands";
const PAGE_DESCRIPTION =
  "Guide to unifying RGB lighting across different brands: picking a primary controller, disabling conflicts and wiring strips correctly.";

const navItems = [
  { title: "Best Picks", url: "/#best-picks" },
  { title: "Reviews", url: "/reviews/" },
  { title: "Comparisons", url: "/comparisons/" },
  { title: "Guides", url: "/guides/" },
  { title: "Device Compatibility", url: "/compatibility/" },
  { title: "FAQ", url: "/faq/" },
];
---

<!doctype html>
<html lang={locale} class="h-full motion-safe:scroll-smooth" data-theme="dark">
  <head>
    <LanguagePreference locale={locale} />
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" href="/favicon.svg" type="image/svg+xml" />
    {generator && <meta name="generator" content={generator} />}
    <title>{PAGE_TITLE}</title>
    <meta name="description" content={PAGE_DESCRIPTION} />
    <meta property="og:title" content={PAGE_TITLE} />
    <meta property="og:type" content="article" />
    <meta property="og:description" content={PAGE_DESCRIPTION} />
    <meta property="og:image" content={pageData.head.image} />
    <meta property="og:url" content={pageData.head.canonical} />
    <meta name="twitter:card" content="summary_large_image" />
    <link rel="canonical" href="https://best-rgb-controller.com/guides/unify-rgb-across-brands/" />

    <GoogleAnalytics />
  </head>
  <body
    class="h-full overflow-x-hidden bg-default pt-24 text-default text-base selection:bg-secondary selection:text-white"
  >
    <Header
      navItems={navItems}
      locale={locale}
      navLabel={copy.nav.ariaLabel}
      openLabel={copy.nav.openLabel}
      closeLabel={copy.nav.closeLabel}
      languageLabel={copy.language.label}
      languageOptions={languageOptions}
    />

    <main class="px-8 pt-16 pb-24">
      <article class="mx-auto max-w-5xl space-y-8">
        <header class="space-y-3">
          <p class="text-xs font-semibold uppercase tracking-[0.25em] text-primary">
            Guide – Cross‑brand RGB Sync
          </p>
          <h1 class="text-balance text-3xl font-extrabold tracking-tight sm:text-4xl">
            How to sync RGB lights across different brands
          </h1>
          <p class="max-w-3xl text-sm text-offset sm:text-base">
            Mixing brands is normal: one vendor for your keyboard, another for your mouse, a
            different one again for your fans and motherboard. This guide shows how to get them as
            unified as possible under one controller.
          </p>
        </header>

        <section class="space-y-3">
          <h2 class="text-lg font-semibold tracking-tight sm:text-xl">
            Step 1 – Pick a primary controller
          </h2>
          <p class="text-sm text-offset sm:text-base">
            Start by choosing one controller to be “in charge” – typically Skydimo, SignalRGB or
            OpenRGB. Then, where possible, disable lighting features in vendor tools so they don&apos;t
            fight over devices.
          </p>
        </section>

        <section class="space-y-3">
          <h2 class="text-lg font-semibold tracking-tight sm:text-xl">
            Step 2 – Group devices logically
          </h2>
          <p class="text-sm text-offset sm:text-base">
            Instead of trying to give each device a unique configuration, group them into logical
            sets: “desk peripherals”, “case interior”, “ambient strips”. Apply similar colours and
            effects within each group for a cleaner result.
          </p>
        </section>

        <section class="space-y-3">
          <h2 class="text-lg font-semibold tracking-tight sm:text-xl">
            Step 3 – Test screen and music sync
          </h2>
          <p class="text-sm text-offset sm:text-base">
            Once basic colours look right, test the screen‑sync and music‑sync modes of your chosen
            controller. If you notice lag or stutter, follow the
            <a
              href="/guides/reduce-screen-sync-lag/"
              class="underline decoration-slate-500/60 decoration-dotted underline-offset-2 hover:text-primary"
            >
              screen‑sync lag reduction guide
            </a>
            to tune things.
          </p>
        </section>

        <footer class="border-t border-slate-700/60 pt-6 text-sm text-offset">
          <p>
            For a bigger‑picture view of why cross‑brand sync is hard, read the
            <a
              href="/rgb-ecosystem/"
              class="ml-1 underline decoration-slate-500/60 decoration-dotted underline-offset-2 hover:text-primary"
            >
              RGB ecosystem overview
            </a>
            .
          </p>
        </footer>
      </article>
    </main>

    <Footer linkLabels={copy.footer.links} />
  </body>
</html>


