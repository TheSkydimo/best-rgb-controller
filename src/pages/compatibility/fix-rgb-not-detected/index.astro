---
import "@fontsource-variable/inter";
import Footer from "~/components/footer.astro";
import GoogleAnalytics from "~/components/google-analytics.astro";
import Header from "~/components/header.astro";
import LanguagePreference from "~/components/language-preference.astro";
import { DEFAULT_LOCALE } from "~/i18n/config";
import { buildPageData } from "~/i18n/page-data";
import "~/styles/index.css";

const { generator, site } = Astro;
const locale = DEFAULT_LOCALE;
const pageData = buildPageData(locale, site);
const { copy, languageOptions } = pageData;

const PAGE_TITLE = "How to Fix RGB Not Detected on Your Motherboard";
const PAGE_DESCRIPTION =
  "Troubleshooting guide for RGB not detected on your motherboard: BIOS settings, headers, cables, controller choice and common pitfalls.";

const navItems = [
  { title: "Best Picks", url: "/#best-picks" },
  { title: "Reviews", url: "/reviews/" },
  { title: "Comparisons", url: "/comparisons/" },
  { title: "Guides", url: "/guides/" },
  { title: "Device Compatibility", url: "/compatibility/" },
  { title: "FAQ", url: "/faq/" },
];
---

<!doctype html>
<html lang={locale} class="h-full motion-safe:scroll-smooth" data-theme="dark">
  <head>
    <LanguagePreference locale={locale} />
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" href="/favicon.svg" type="image/svg+xml" />
    {generator && <meta name="generator" content={generator} />}
    <title>{PAGE_TITLE}</title>
    <meta name="description" content={PAGE_DESCRIPTION} />
    <meta property="og:title" content={PAGE_TITLE} />
    <meta property="og:type" content="article" />
    <meta property="og:description" content={PAGE_DESCRIPTION} />
    <meta property="og:image" content={pageData.head.image} />
    <meta property="og:url" content={pageData.head.canonical} />
    <meta name="twitter:card" content="summary_large_image" />
    <link rel="canonical" href="https://best-rgb-controller.com/compatibility/fix-rgb-not-detected/" />

    <GoogleAnalytics />
  </head>
  <body
    class="h-full overflow-x-hidden bg-default pt-24 text-default text-base selection:bg-secondary selection:text-white"
  >
    <Header
      navItems={navItems}
      locale={locale}
      navLabel={copy.nav.ariaLabel}
      openLabel={copy.nav.openLabel}
      closeLabel={copy.nav.closeLabel}
      languageLabel={copy.language.label}
      languageOptions={languageOptions}
    />

    <main class="px-8 pt-16 pb-24">
      <article class="mx-auto max-w-7xl space-y-8">
        <header class="space-y-3">
          <p class="text-xs font-semibold uppercase tracking-[0.25em] text-primary">
            Compatibility – Motherboard RGB Not Detected
          </p>
          <h1 class="text-balance text-3xl font-extrabold tracking-tight sm:text-4xl">
            How to fix RGB not detected on your motherboard
          </h1>
          <p class="max-w-3xl text-sm text-offset sm:text-base">
            If your controller doesn&apos;t show motherboard RGB zones or strips connected to its
            headers, walk through this checklist before assuming the hardware is dead.
          </p>
        </header>

        <section class="space-y-3">
          <h2 class="text-lg font-semibold tracking-tight sm:text-xl">
            1. Confirm BIOS and firmware settings
          </h2>
          <p class="text-sm text-offset sm:text-base">
            Many boards expose toggles for onboard RGB and header behaviour in BIOS. Make sure RGB
            is enabled and not restricted to vendor‑only control, then update to a stable BIOS
            release if you are several versions behind.
          </p>
        </section>

        <section class="space-y-3">
          <h2 class="text-lg font-semibold tracking-tight sm:text-xl">
            2. Check headers, orientation and voltage
          </h2>
          <p class="text-sm text-offset sm:text-base">
            Double‑check that strips and hubs are connected to the correct 12 V RGB or 5 V ARGB
            headers and that the arrow or “data” pin is aligned correctly. Mixing 12 V and 5 V
            hardware can permanently damage strips.
          </p>
          <p class="text-sm text-offset sm:text-base">
            If you are unsure about header types, start with the
            <a
              href="/compatibility/led-strip-guide/"
              class="underline decoration-slate-500/60 decoration-dotted underline-offset-2 hover:text-primary"
            >
              LED strip support guide
            </a>
            .
          </p>
        </section>

        <section class="space-y-3">
          <h2 class="text-lg font-semibold tracking-tight sm:text-xl">
            3. Test with vendor software first
          </h2>
          <p class="text-sm text-offset sm:text-base">
            Before blaming a third‑party controller, install your motherboard vendor&apos;s own RGB
            tool and check whether it can see and control the LEDs. If even the official software
            fails, you may be looking at a hardware, cable or BIOS issue rather than a controller
            limitation.
          </p>
        </section>
      </article>
    </main>

    <Footer linkLabels={copy.footer.links} />
  </body>
</html>


